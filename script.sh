go mod init (nama project) maka akan keluar file go.mod

go build
untuk mencompile aplikasi

go run (nama aplikasi)
untuk menjalankan secara langsung tanpa build tetapi tidak menyimpan aplikasi seperti build

untuk menjalankan aplikasi golang di windows
.\belajar-golang.exe

untuk menjalankan aplikasi golang di linux/macOs
belajar-golang

untuk setiap nama function tidak boleh sama antar aplikasi

function untuk string
len = untuk menghitung panjang karakter
"string"[number] = untuk mengambil karakter sesuai urutan (seperti array)

untuk variabel ada beberapa cara untuk membuat variabel dan tidak variabel harus dipakai agar tidak error

tipe data pada golang akan otomatis mengenali setiap datanya tanpa harus menambhakan tipe datanya

:= untuk short style variabel

diberi _ apabila tidak menggunakan salah satu index

untuk membuat sebuah function, var, sctructch, interface dari package lain maka diharus kan menggunakan huruf besar diawal agar terbaca saat di gunakan di package lain

any = data kosong

slice mirip dengan array di js
